import 'package:flutter/material.dart';

class GlobalPadding {
  GlobalPadding._();

  static const EdgeInsets padding16All = EdgeInsets.all(16);
  static const EdgeInsets padding20All = EdgeInsets.all(20);
  static const EdgeInsets padding24All = EdgeInsets.all(24);
  static const EdgeInsets padding30All = EdgeInsets.all(30);
}
