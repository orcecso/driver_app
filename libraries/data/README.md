# Data Module
Contains Models, Repositories, and Data Sources 

## Usage

You can import this module by adding

```dart
import 'package:data/data.dart';
```

By doing so, you will be able to use the files inside `models`, `datasources`, and `repositories` folder.
